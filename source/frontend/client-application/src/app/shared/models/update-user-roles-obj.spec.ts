import { UpdateUserRolesObj } from './update-user-roles-obj';

describe('UpdateUserRolesObj', () => {
  it('should create an instance', () => {
    expect(new UpdateUserRolesObj()).toBeTruthy();
  });
});
