import { PermissionUserInfo } from './permission-user-info';

describe('PermissionUserInfo', () => {
  it('should create an instance', () => {
    expect(new PermissionUserInfo()).toBeTruthy();
  });
});
