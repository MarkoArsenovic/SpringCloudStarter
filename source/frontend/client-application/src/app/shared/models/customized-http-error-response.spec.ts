import { CustomizedHttpErrorResponse } from './customized-http-error-response';

describe('CustomizedHttpErrorResponse', () => {
  it('should create an instance', () => {
    expect(new CustomizedHttpErrorResponse()).toBeTruthy();
  });
});
